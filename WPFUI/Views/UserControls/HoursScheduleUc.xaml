<v:HoursScheduleUcBase
    x:Class="WPFUI.Views.UserControls.HoursScheduleUc"
    xmlns:v="clr-namespace:WPFUI.Views.UserControls"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    mc:Ignorable="d"
    d:DesignHeight="120" d:DesignWidth="800"
    x:Name="root">
    <StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
            <Label Content="{Binding Text, ElementName=root}" Width="NaN" VerticalAlignment="Center" Margin="0,0,10,0" />
            <Button x:Name="AllButton" Content="All" Style="{DynamicResource MaterialDesignFlatButton}" Padding="4,0" />
            <Button x:Name="NoneButton" Content="None" Style="{DynamicResource MaterialDesignFlatButton}" Padding="4,0" />
        </StackPanel>
        <ItemsControl x:Name="HoursItems">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <CheckBox
                        Content="{Binding Label}"
                        IsChecked="{Binding IsChecked, Mode=TwoWay}"
                        ToolTip="{Binding Label, StringFormat='Hour {0}:00 - {0}:59'}"
                        Width="42"
                        Margin="0,0,4,4"
                        HorizontalAlignment="Center" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</v:HoursScheduleUcBase>
